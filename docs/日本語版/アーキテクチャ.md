# Speckit アーキテクチャと設計思想

このドキュメントでは、Speckitシステムの仕組みと設計思想を深く解説します。

## 目次

- [システムの全体像](#システムの全体像)
- [設計思想](#設計思想)
- [ディレクトリ構造の詳細](#ディレクトリ構造の詳細)
- [テンプレートシステム](#テンプレートシステム)
- [スクリプトの仕組み](#スクリプトの仕組み)
- [フェーズの詳細設計](#フェーズの詳細設計)
- [拡張性](#拡張性)

---

## システムの全体像

### レイヤーアーキテクチャ

Speckitは3層のアーキテクチャで構成されています:

```
┌─────────────────────────────────────────┐
│  User Interface Layer                   │
│  (スラッシュコマンド)                    │
│  /speckit.specify, /speckit.plan, etc.  │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│  Orchestration Layer                    │
│  (コマンド定義 in .claude/commands/)    │
│  - ワークフロー制御                      │
│  - バリデーション                        │
│  - 段階的実行                            │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│  Execution Layer                        │
│  (スクリプト in .specify/scripts/)      │
│  - ファイル操作                          │
│  - Git操作                               │
│  - 環境セットアップ                      │
└───────────────┬─────────────────────────┘
                │
┌───────────────▼─────────────────────────┐
│  Data Layer                             │
│  (仕様書、計画書、タスクリスト)          │
│  specs/[feature]/                       │
└─────────────────────────────────────────┘
```

### 情報の流れ

```
ユーザー入力
    ↓
コマンド解析
    ↓
テンプレート読み込み
    ↓
AIによる生成・変換
    ↓
バリデーション
    ↓
ファイル出力
    ↓
次のコマンドへ
```

---

## 設計思想

### 1. 段階的詳細化 (Progressive Refinement)

**原則**: 一度にすべてを決めず、段階的に詳細度を上げる

```
抽象度: 高 ────────────────────────────> 低

specify      clarify       plan        tasks      implement
(何を)       (明確化)     (どうやって)  (細分化)    (コード)

ビジネス価値  ← 技術選定  →  実行レベル
```

**利点**:
- 早期に方向性を確認できる
- 手戻りのコストを削減
- ステークホルダーごとに適切なレベルで関与

**実装**:

```markdown
# specify フェーズ
「ユーザーは2分以内にアカウント作成できる」

# plan フェーズ
「FastAPI + PostgreSQL + bcrypt」

# tasks フェーズ
「T001: Userモデルを作成 src/models/user.py」

# implement フェーズ
「class User(Base): ...」
```

### 2. 関心の分離 (Separation of Concerns)

**原則**: WHAT (何) と HOW (どうやって) を明確に分離

```
┌──────────────────┐
│  spec.md         │  WHAT & WHY
│  (仕様書)         │  - ユーザーの課題
│                  │  - ビジネス価値
│                  │  - 成功基準
└──────────────────┘
         ↓
┌──────────────────┐
│  plan.md         │  HOW (技術)
│  (計画書)         │  - 技術スタック
│                  │  - アーキテクチャ
│                  │  - データモデル
└──────────────────┘
         ↓
┌──────────────────┐
│  tasks.md        │  HOW (実行)
│  (タスクリスト)   │  - 具体的な作業
│                  │  - ファイル単位
└──────────────────┘
```

**利点**:
- ビジネス側は仕様書だけを理解すればよい
- 技術者は技術詳細に集中できる
- 変更の影響範囲が明確

### 3. ユーザーストーリー中心設計

**原則**: すべてをユーザーストーリーを軸に整理

```
User Story 1 (P1)
    ├── 機能要件 (FR-001, FR-002, ...)
    ├── 成功基準 (SC-001, SC-002, ...)
    ├── データモデル (User, Session)
    ├── APIエンドポイント (POST /auth/login)
    └── タスク (T001, T002, ..., T010)

User Story 2 (P2)
    ├── 機能要件 (FR-011, FR-012, ...)
    └── タスク (T011, T012, ..., T015)
```

**利点**:
- 優先度付けが明確 (P1 → P2 → P3)
- 各ストーリーが独立してリリース可能
- トレーサビリティ (なぜこのコードが必要か明確)

**実装**:

```markdown
# spec.md
### User Story 1 - Googleログイン (Priority: P1)

# tasks.md
## Phase 3: User Story 1 - Googleログイン (P1)
- [ ] T006 [US1] OAuth2設定を追加 src/services/auth.py
- [ ] T007 [US1] ログインエンドポイント src/api/auth.py
```

### 4. 憲章による制約 (Constitution as Constraints)

**原則**: プロジェクト全体に適用される不変の原則を明文化

```
┌─────────────────────────────────────────┐
│  constitution.md                        │
│  (プロジェクト憲章)                      │
│  - TDD必須                               │
│  - ライブラリファースト                  │
│  - API契約優先                           │
└──────────────┬──────────────────────────┘
               │
               │ すべての仕様・計画はこれに準拠
               │
      ┌────────┼────────┐
      ▼        ▼        ▼
   spec.md  plan.md  tasks.md
```

**利点**:
- チーム全体の合意を強制
- 品質基準が一貫
- 新メンバーへのオンボーディングが容易

### 5. テンプレート駆動生成

**原則**: すべてのドキュメントはテンプレートから生成

```
.specify/templates/
├── spec-template.md      → specs/[feature]/spec.md
├── plan-template.md      → specs/[feature]/plan.md
├── tasks-template.md     → specs/[feature]/tasks.md
└── checklist-template.md → specs/[feature]/checklists/[domain].md
```

**利点**:
- 一貫性のある構造
- 必須セクションの漏れを防止
- カスタマイズ可能

---

## ディレクトリ構造の詳細

### ルートディレクトリ

```
my-project/
├── .specify/              # Speckitシステムファイル (ユーザー編集不要)
├── .claude/               # Claude Code設定 (コマンド定義)
├── specs/                 # 機能ごとのワークスペース (自動生成)
├── src/                   # 実際のソースコード (実装される)
├── tests/                 # テストコード (実装される)
├── .gitignore             # Git無視ファイル
├── CLAUDE.md              # Claude向けガイド
└── README.md              # プロジェクト概要
```

### .specify/ ディレクトリ

**役割**: Speckitシステムの核となるファイル群

```
.specify/
├── memory/
│   └── constitution.md           # プロジェクト憲章 (不変の原則)
│
├── scripts/
│   └── bash/
│       ├── common.sh             # 共通ユーティリティ関数
│       ├── create-new-feature.sh # 機能ブランチ作成
│       ├── setup-plan.sh         # 計画フェーズ初期化
│       ├── check-prerequisites.sh # 前提条件確認
│       └── update-agent-context.sh # AI エージェントコンテキスト更新
│
└── templates/
    ├── spec-template.md          # 仕様書テンプレート
    ├── plan-template.md          # 計画書テンプレート
    ├── tasks-template.md         # タスクリストテンプレート
    ├── checklist-template.md     # チェックリストテンプレート
    └── agent-file-template.md    # エージェント用ファイルテンプレート
```

### .claude/ ディレクトリ

**役割**: スラッシュコマンドの定義

```
.claude/
└── commands/
    ├── speckit.specify.md        # /speckit.specify コマンド定義
    ├── speckit.clarify.md        # /speckit.clarify コマンド定義
    ├── speckit.plan.md           # /speckit.plan コマンド定義
    ├── speckit.tasks.md          # /speckit.tasks コマンド定義
    ├── speckit.analyze.md        # /speckit.analyze コマンド定義
    ├── speckit.implement.md      # /speckit.implement コマンド定義
    ├── speckit.constitution.md   # /speckit.constitution コマンド定義
    ├── speckit.checklist.md      # /speckit.checklist コマンド定義
    └── speckit.taskstoissues.md  # /speckit.taskstoissues コマンド定義
```

**コマンドファイルの構造**:

```markdown
---
description: コマンドの説明
handoffs:
  - label: 次のステップの名前
    agent: 次のコマンド
    prompt: デフォルトプロンプト
---

## User Input
$ARGUMENTS (ユーザーが入力したパラメータ)

## Outline
1. 実行ステップ1
2. 実行ステップ2
...

## Key Rules
- 重要な制約事項
```

### specs/ ディレクトリ

**役割**: 機能ごとのワークスペース

```
specs/
├── 1-health-check/           # 機能1: ヘルスチェック
│   ├── spec.md               # 機能仕様
│   ├── plan.md               # 実装計画
│   ├── tasks.md              # タスクリスト
│   ├── research.md           # 技術調査
│   ├── data-model.md         # データモデル
│   ├── quickstart.md         # クイックスタート
│   ├── contracts/            # API契約
│   │   └── health.openapi.yaml
│   └── checklists/           # 品質チェックリスト
│       ├── security.md
│       └── ux.md
│
├── 2-user-auth/              # 機能2: ユーザー認証
│   ├── spec.md
│   ├── plan.md
│   └── ...
│
└── 3-payment/                # 機能3: 決済
    └── ...
```

**ブランチとディレクトリの対応**:

```
Git Branch: 1-health-check
Directory:  specs/1-health-check/

Git Branch: 2-user-auth
Directory:  specs/2-user-auth/
```

---

## テンプレートシステム

### テンプレートの役割

テンプレートは**構造を標準化**し、**必須セクションを強制**します。

### spec-template.md の設計

```markdown
# Feature Specification: [FEATURE NAME]

## User Scenarios & Testing *(mandatory)*

### User Story 1 - [Title] (Priority: P1)

[Description]

**Why this priority**: [Reason]
**Independent Test**: [How to test]

**Acceptance Scenarios**:
1. **Given** [state], **When** [action], **Then** [outcome]

## Requirements *(mandatory)*

### Functional Requirements
- **FR-001**: System MUST [capability]

## Success Criteria *(mandatory)*

- **SC-001**: [Measurable outcome]
```

**プレースホルダー**:
- `[FEATURE NAME]`: 機能名
- `[Title]`: ストーリーのタイトル
- `*(mandatory)*`: 必須セクション

**AIの役割**:
- プレースホルダーを具体的な内容に置き換え
- ユーザー入力からストーリーを抽出
- 機能要件と成功基準を生成

### plan-template.md の設計

```markdown
# Implementation Plan: [FEATURE]

## Technical Context

**Language/Version**: [e.g., Python 3.11 or NEEDS CLARIFICATION]
**Primary Dependencies**: [e.g., FastAPI or NEEDS CLARIFICATION]
...

## Constitution Check

*GATE: Must pass before Phase 0*

[憲章との整合性チェック]

## Project Structure

### Source Code

```text
[ディレクトリツリー]
```
```

**NEEDS CLARIFICATION パターン**:

不明な技術選定は `NEEDS CLARIFICATION` マーカーを使用:

```markdown
**Storage**: NEEDS CLARIFICATION (データベースの種類が未定)
```

これがPhase 0 (研究フェーズ) で解決されます。

### tasks-template.md の設計

```markdown
# Tasks: [FEATURE]

## Phase 1: Setup
- [ ] T001 [Task description]

## Phase N: User Story X - [Title] (P1)

**Story Goal**: [What this story achieves]
**Independent Test**: [How to verify independently]

### Implementation Tasks
- [ ] TXXX [P?] [USX] [Description] [file path]

## Dependencies

[依存関係グラフ]

## Parallel Execution Examples

[並列実行の機会]
```

**タスクフォーマットの厳格性**:

```
- [ ] [TaskID] [P?] [Story?] Description filepath
```

この形式により:
- 自動パース可能
- GitHub Issue変換が容易
- 進捗トラッキングが可能

---

## スクリプトの仕組み

### create-new-feature.sh

**役割**: 新しい機能ブランチとディレクトリを作成

**フロー**:

```bash
1. 引数解析
   --number N
   --short-name "feature-name"
   --json  # JSON出力モード

2. ブランチ名生成
   "N-short-name"  # 例: "1-user-auth"

3. 既存ブランチ確認
   - リモート: git ls-remote
   - ローカル: git branch
   - ディレクトリ: specs/*/

4. 番号の重複回避
   最大番号を検出 → +1

5. ブランチ作成
   git checkout -b N-short-name

6. ディレクトリ作成
   mkdir -p specs/N-short-name

7. 初期ファイル作成
   cp .specify/templates/spec-template.md specs/N-short-name/spec.md

8. JSON出力
   {"BRANCH_NAME": "...", "FEATURE_DIR": "...", "SPEC_FILE": "..."}
```

**JSON出力モード**:

```json
{
  "BRANCH_NAME": "1-user-auth",
  "FEATURE_DIR": "/absolute/path/specs/1-user-auth",
  "SPEC_FILE": "/absolute/path/specs/1-user-auth/spec.md",
  "SHORT_NAME": "user-auth"
}
```

AIはこのJSONをパースして、ファイルパスを取得します。

### check-prerequisites.sh

**役割**: コマンド実行前の前提条件を確認

**モード**:

1. **--json**: JSON形式で出力
2. **--require-tasks**: tasks.md が必須
3. **--include-tasks**: tasks.md の内容を含める
4. **--paths-only**: パスのみ返す (ドキュメント内容なし)

**フロー**:

```bash
1. 現在のブランチ確認
   git branch --show-current

2. ブランチ名から番号抽出
   "1-user-auth" → "1"

3. FEATURE_DIR 特定
   specs/1-user-auth/

4. 必須ファイル確認
   - spec.md (常に必須)
   - plan.md (--require-planの場合)
   - tasks.md (--require-tasksの場合)

5. AVAILABLE_DOCS リスト生成
   [spec.md, plan.md, research.md, ...]

6. JSON出力
   {
     "FEATURE_DIR": "...",
     "FEATURE_SPEC": "...",
     "IMPL_PLAN": "...",
     "TASKS": "...",
     "AVAILABLE_DOCS": [...]
   }
```

### update-agent-context.sh

**役割**: AI エージェント用のコンテキストファイルを更新

**対応エージェント**:
- `claude`: Claude Code
- `cursor`: Cursor
- `copilot`: GitHub Copilot

**フロー**:

```bash
1. エージェント検出
   引数 "claude" → Claude Code

2. コンテキストファイル特定
   .claude/context.md (Claudeの場合)

3. 現在の技術スタックを抽出
   plan.md から言語、フレームワーク、ライブラリを取得

4. マーカー間に挿入
   <!-- AUTO-GENERATED: TECH STACK -->
   - Python 3.11
   - FastAPI
   - PostgreSQL
   <!-- END AUTO-GENERATED -->

5. 手動追加部分は保持
   マーカー外の内容は変更しない
```

**利点**:
- AIエージェントが最新の技術スタックを認識
- 計画変更時に自動更新

---

## フェーズの詳細設計

### Phase 0: Research (調査研究)

**目的**: 技術的な不確実性を解消

**入力**:
- `plan.md` の `NEEDS CLARIFICATION` マーカー
- 仕様から導出された技術要件

**処理**:

```
For each NEEDS CLARIFICATION:
  1. 調査タスクを生成
     "Research authentication library for Python FastAPI"

  2. ベストプラクティスを収集
     - 公式ドキュメント
     - 業界標準
     - セキュリティ考慮事項

  3. 選択肢を評価
     Option A: authlib (pros/cons)
     Option B: python-jose (pros/cons)

  4. 推奨を決定
     Decision: authlib
     Rationale: FastAPI統合、OAuth2フルサポート
```

**出力**:
- `research.md`: すべての決定とその根拠

**例**:

```markdown
# Research: 認証ライブラリ

## Decision: authlib

**Rationale**:
- FastAPI公式で推奨
- OAuth2 1.0, 2.0フルサポート
- アクティブに開発されている

**Alternatives considered**:
- python-jose: JWTのみ、OAuth2の全機能なし
- OAuthLib: 低レベルすぎ、FastAPI統合なし
```

### Phase 1: Design (設計)

**目的**: データモデルとAPI契約を定義

**サブフェーズ**:

#### 1A. データモデル設計

**入力**:
- `spec.md` の Key Entities
- 機能要件 (FR-XXX)

**処理**:

```
For each Entity:
  1. フィールド定義
     - ID (主キー)
     - 属性 (型、制約)
     - リレーション (1:N, N:M)

  2. バリデーションルール
     - 一意性制約
     - 必須フィールド
     - 形式検証

  3. 状態遷移
     (ある場合)
```

**出力**:
- `data-model.md`

**例**:

```markdown
## User

- id: UUID (PK)
- email: String (unique, indexed)
- created_at: DateTime (auto)

**Validation**:
- email must be valid format (RFC 5322)

**Relationships**:
- User 1:N Session
```

#### 1B. API契約定義

**入力**:
- `spec.md` の機能要件
- データモデル

**処理**:

```
For each 機能要件:
  1. ユーザーアクションを特定
     "ユーザーはログインできる" → POST /auth/login

  2. エンドポイント定義
     - HTTPメソッド
     - パス
     - リクエストボディ
     - レスポンス

  3. OpenAPI形式で記述
```

**出力**:
- `contracts/*.openapi.yaml`

**例**:

```yaml
paths:
  /auth/login:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                provider:
                  type: string
                  enum: [google, github]
      responses:
        '302':
          description: Redirect to provider
```

### Phase 2: Task Breakdown (タスク分解)

**目的**: 実行可能な単位にタスクを分解

**アルゴリズム**:

```
1. Setup Phase生成
   - プロジェクト構造
   - 依存関係インストール
   - 環境設定

2. Foundational Phase生成
   - 全ストーリーで共通のモデル・サービス

3. For each User Story (優先度順):
   a. ストーリー専用のタスクを生成
      - そのストーリーで必要なモデル
      - そのストーリーで必要なサービス
      - そのストーリーで必要なエンドポイント

   b. 依存関係を分析
      - 同じファイル → 順次実行
      - 異なるファイル → [P] マーカー

   c. タスクID割り当て
      T001, T002, ... (実行順)

4. Polish Phase生成
   - エラーハンドリング
   - ログ設定
   - テスト
```

**依存関係グラフ**:

```
Setup → Foundational → US1 → US2 → Polish
                    ↘ US2 ↗ (並列可能な場合)
```

### Phase 3: Implementation (実装)

**目的**: タスクを順次実行してコードを生成

**実行エンジン**:

```python
def execute_tasks(tasks):
    for phase in tasks.phases:
        print(f"Phase {phase.number}: {phase.name} 開始...")

        for task in phase.tasks:
            if task.is_parallel and has_parallel_siblings(task):
                # 並列実行
                results = execute_parallel([task] + get_siblings(task))
                for result in results:
                    if result.failed:
                        handle_error(result)
            else:
                # 順次実行
                result = execute_task(task)
                if result.failed:
                    stop_execution()
                    return

            # tasks.mdを更新
            mark_task_complete(task)

        print(f"Phase {phase.number} 完了")

    print("🎉 全実装完了")
```

**タスク実行**:

```python
def execute_task(task):
    # 1. タスク情報をパース
    description = task.description  # "Userモデルを作成"
    filepath = task.filepath        # "src/models/user.py"
    story_label = task.story        # "US1"

    # 2. コンテキスト読み込み
    if story_label:
        story = load_user_story(story_label)  # spec.mdから
        requirements = get_story_requirements(story)
    else:
        requirements = []

    model_spec = load_data_model(filepath)  # data-model.mdから

    # 3. コード生成
    code = generate_code(
        description=description,
        requirements=requirements,
        model_spec=model_spec,
        tech_stack=load_plan()
    )

    # 4. ファイル書き込み
    write_file(filepath, code)

    return TaskResult(success=True)
```

---

## 拡張性

### カスタムコマンドの追加

新しいスラッシュコマンドを追加できます:

**例**: `/speckit.deploy` コマンド

```bash
# 1. コマンド定義を作成
vim .claude/commands/speckit.deploy.md
```

```markdown
---
description: Deploy the implemented feature to production
---

## Outline

1. Run tests
2. Build Docker image
3. Push to registry
4. Update Kubernetes manifests
5. Apply deployment
```

**使用**:

```bash
/speckit.deploy
```

### カスタムテンプレートの追加

プロジェクト固有のテンプレートを追加:

**例**: API仕様書テンプレート

```bash
# 1. テンプレート作成
vim .specify/templates/api-spec-template.md
```

```markdown
# API Specification: [FEATURE]

## Endpoints

### [METHOD] [PATH]

**Description**: [What this endpoint does]

**Request**:
```json
{
  "field": "value"
}
```

**Response**:
```json
{
  "status": "success"
}
```
```

**コマンドで使用**:

```bash
# 2. カスタムコマンド作成
vim .claude/commands/speckit.apispec.md
```

### 憲章のカスタマイズ

プロジェクトに合わせて憲章を調整:

```markdown
# My Project Constitution

## Core Principles

### I. [Your Principle]

[Description]

**Requirements**:
- [Requirement 1]
- [Requirement 2]

### II. [Another Principle]

...

## Technology Constraints

- Python 3.11+ only
- PostgreSQL for relational data
- Redis for caching

## Quality Gates

- Code coverage >= 80%
- All PRs require 2 approvals
- No direct commits to main
```

### 多言語対応

テンプレートを多言語化:

```
.specify/templates/
├── spec-template.md          # 英語 (デフォルト)
├── spec-template.ja.md       # 日本語
└── spec-template.zh.md       # 中国語
```

**使用**:

```bash
# 言語を環境変数で指定
export SPECKIT_LANG=ja
/speckit.specify ユーザー認証機能
```

---

## まとめ: Speckitの強み

### 1. 構造化された開発プロセス

曖昧さを段階的に解消し、品質の高い成果物を生成

### 2. トレーサビリティ

ユーザーストーリー → 要件 → タスク → コード

すべてが繋がっているため、「なぜこのコードが必要か」が明確

### 3. スケーラビリティ

小さな機能から大規模システムまで対応

### 4. チーム協調

憲章、チェックリスト、GitHub連携により、チーム開発をサポート

### 5. AI活用の最適化

テンプレート駆動でAIの生成品質を向上

---

これでSpeckitの完全な理解が得られました。実際のプロジェクトで活用してみてください！
